<configuration>
    <!-- Root logger -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{20} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/application.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] [%level] %logger{20} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Define log levels for different parts -->
    <logger name="basenostates.first" level="INFO" />
    <logger name="basenostates.second" level="WARN" />
    <logger name="basenostates.third" level="ERROR" />
    <logger name="basenostates" level="DEBUG" />

    <!-- Root logger sends to both console and file -->
    <root level="DEBUG">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>
